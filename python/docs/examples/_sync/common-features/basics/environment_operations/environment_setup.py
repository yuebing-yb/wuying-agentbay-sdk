# DO NOT EDIT THIS FILE MANUALLY.
# This file is auto-generated from the _async directory.

"""Environment Setup Example"""
import os
import sys

sys.path.append(os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))))))

from agentbay import AgentBay
from agentbay import CreateSessionParams


def main():
    print("=== Environment Setup ===\n")
    client = AgentBay()
    session = None

    try:
        session_result = client.create(CreateSessionParams(image_id="linux_latest"))
        session = session_result.session

        # Set environment variable
        result = session.command.execute_command("export MY_VAR='test' && echo $MY_VAR")
        print(f"1. Set var: {result.output}")

        # Check PATH
        result = session.command.execute_command("echo $PATH | head -c 100")
        print(f"2. PATH: {result.output}...")

        # Check HOME
        result = session.command.execute_command("echo $HOME")
        print(f"3. HOME: {result.output}")

        print("\n=== Completed ===")

    finally:
        if session:
            client.delete(session)


if __name__ == "__main__":
    main()

