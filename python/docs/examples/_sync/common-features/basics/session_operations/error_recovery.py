# DO NOT EDIT THIS FILE MANUALLY.
# This file is auto-generated from the _async directory.

"""Error Recovery Example"""
import os
import sys

sys.path.append(os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))))))

from agentbay import AgentBay
from agentbay import CreateSessionParams


def main():
    print("=== Error Recovery ===\n")
    client = AgentBay()
    session = None

    try:
        session_result = client.create(CreateSessionParams(image_id="linux_latest"))
        session = session_result.session

        # Try command that might fail
        result = session.command.execute_command("ls /nonexistent 2>&1 || echo 'Recovered'")
        print(f"1. Recovery: {result.output}")

        # Continue with valid operations
        result = session.command.execute_command("echo 'Continuing...'")
        print(f"2. Continue: {result.output}")

        print("\n=== Completed ===")

    finally:
        if session:
            client.delete(session)


if __name__ == "__main__":
    main()

